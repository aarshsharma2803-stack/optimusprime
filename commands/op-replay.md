---
description: Replay any past session like a debugger — every decision, failure, scope block, and loop in order.
---
Run `op replay --list` to see past sessions, then `op replay --summary` for the most recent.
